def fake_web_search(query: str) -> str:
    """Tool placeholder para desarrollo local sin dependencias externas."""

    return (
        "Hallazgos simulados para la consulta: "
        f"{query}. Reemplaza este tool por Tavily/Wikipedia/document loaders."
    )
